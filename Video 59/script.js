/**
 * User Input
 * perform wrong operation 
 * + --> -
 * * --> +
 * - --> /
 * / --> /
 */

let a = prompt(parseInt("Enter First No"))
let b = prompt(parseInt("Enter First No"))
let o = prompt(parseInt("Enter operation to perform"))

let rand = Math.random()

if (rand <0.1 ) {
    document.write(eval(`$a $o $b`))
} else {
    
}